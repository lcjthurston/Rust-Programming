fn main() {
    println!("Hello, world!");
}

// The ! after println refers to a macro and not a function.
// rustc is fine for compiling small programs, but use cargo for real world programs.
// In cmd, the 'dir /B' command displays only the names of the files.
// In Rust, packages of code are called crates.  